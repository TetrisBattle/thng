{
	"name": "test",
	"version": "1.0.0",
	"author": "Thien Nguyen",
	"license": "MIT",
	"type": "module",
	"eslintConfig": {
		"extends": [
			"plugin:thng/recommendedReact"
		]
	},
	"scripts": {
		"dev": "vite",
		"build": "tsc && vite build",
		"test": "vitest --coverage",
		"lint": "eslint src --ext ts,tsx --report-unused-disable-directives --max-warnings 0",
		"preview": "vite preview"
	},
	"dependencies": {
		"@thng/react": "^1.0.5",
		"react": "^18.2.0",
		"react-dom": "^18.2.0"
	},
	"devDependencies": {
		"@thng/dev": "^1.2.4"
	}
}
